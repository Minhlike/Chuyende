{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "contract_id": "SCHEMA-EXPERIMENTAL-SOURCE-V1.0",
  "version": "1.0.0",
  "title": "Experimental Source and Empirical Evidence Provenance JSON Schema",
  "type": "object",
  "required": [
    "claim_id",
    "stage",
    "run_id",
    "dataset",
    "split_id",
    "git_commit_sha",
    "protocol_version",
    "protocol_sha256",
    "command_executed",
    "environment",
    "metrics_artifact_path",
    "metrics_artifact_sha256",
    "test_firewall_state",
    "evidence_class"
  ],
  "properties": {
    "claim_id": { "type": "string" },
    "stage": { "type": "string", "enum": ["STAGE_A1", "STAGE_A2", "STAGE_B", "STAGE_C", "PREEXECUTION_AUDIT"] },
    "run_id": { "type": "string" },
    "dataset": { "type": "string" },
    "split_id": { "type": "string" },
    "seed": { "type": ["integer", "null"] },
    "git_commit_sha": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "git_branch": { "type": "string" },
    "git_dirty": { "type": "boolean" },
    "protocol_version": { "type": "string" },
    "protocol_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "graph_contract_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "raw_to_graph_mapping_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "raw_dataset_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "selected_train_membership_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "selected_val_membership_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "command_executed": { "type": "string" },
    "working_directory": { "type": "string" },
    "timestamp_start": { "type": "string" },
    "timestamp_end": { "type": "string" },
    "environment": {
      "type": "object",
      "required": ["python_version", "platform"],
      "properties": {
        "python_version": { "type": "string" },
        "pytorch_version": { "type": "string" },
        "cuda_version": { "type": "string" },
        "device_name": { "type": "string" },
        "platform": { "type": "string" }
      }
    },
    "stdout_log_path": { "type": "string" },
    "stdout_log_sha256": { "type": "string" },
    "metrics_artifact_path": { "type": "string" },
    "metrics_artifact_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "checkpoint_path": { "type": "string" },
    "checkpoint_sha256": { "type": "string" },
    "test_firewall_state": {
      "type": "object",
      "required": ["test_opened", "test_feature_reads", "test_label_reads", "test_metrics"],
      "properties": {
        "test_opened": { "type": "boolean", "const": false },
        "test_feature_reads": { "type": "integer", "const": 0 },
        "test_label_reads": { "type": "integer", "const": 0 },
        "test_metrics": { "type": "integer", "const": 0 }
      }
    },
    "evidence_class": {
      "type": "string",
      "enum": ["REAL_EMPIRICAL", "PREEXECUTION_AUDIT", "NON_EMPIRICAL_TEST_FIXTURE"]
    }
  }
}
